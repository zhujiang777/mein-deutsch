{
  "name": "mein-deutsch",
  "private": true,
  "type": "module",
  "scripts": {
    "content:index": "node scripts/gen_content_index.mjs",
    "test": "node --test tests/*.test.mjs",
    "worker:dev": "npx wrangler dev --config worker/wrangler.toml --var ALLOWED_ORIGINS:http://localhost:8420",
    "worker:deploy": "npx wrangler deploy --config worker/wrangler.toml"
  }
}
